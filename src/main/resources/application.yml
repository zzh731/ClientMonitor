spring:
  datasource:
    #   数据源基本配置
    username: zzh
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/client_manager

mybatis:
                      # 指定全局配置文件位置
                      config-location: classpath:mybatis/mybatis-config.xml
                      # 指定sql映射文件位置
                      mapper-locations: classpath:mybatis/mapper/*.xml